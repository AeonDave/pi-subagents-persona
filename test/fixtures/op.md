---
name: op
description: An operator (no `persona: true`) used by tests — must NOT load as a persona.
systemPromptMode: replace
tools: read, grep, bash
---
You are an operator (test fixture). Not a persona.
